services:
  api:
    build: ./backend
    container_name: yakini-api
    restart: unless-stopped
    environment:
      SQLITE_DB_PATH: /data/yakini.sqlite
      CORS_ORIGINS: http://localhost:5173
      SECRET_KEY: ${SECRET_KEY}
      INITIAL_USERNAME: ${INITIAL_USERNAME}
      INITIAL_PASSWORD: ${INITIAL_PASSWORD}
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/data

volumes:
  sqlite_data:
